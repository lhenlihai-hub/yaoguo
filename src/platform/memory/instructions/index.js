const {
  InstructionMemoryService,
  DEFAULT_INSTRUCTION_MEMORY_CONFIG
} = require("./instructionMemoryService");

module.exports = {
  InstructionMemoryService,
  DEFAULT_INSTRUCTION_MEMORY_CONFIG
};
